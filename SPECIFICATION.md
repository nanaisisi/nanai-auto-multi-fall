# 並列テトリス風自動処理ゲーム 仕様設計書 (nanai-auto-multi-fall)

## 1. 概要 (Overview)
本プロジェクトは、RustおよびBevyエンジンを用いた「並列テトリス風」の自動パズルシミュレーション・ゲームです。
複数のレーン（フィールド）が境界を挟んで並列に配置され、3セルで構成されるブロック（直線型・L字型）が自動的に生成・落下・最適配置（自動処理）されていきます。

---

## 2. フィールド構造 (Field Architecture)

### 2.1 レーン構成
- **レーン数 ($N$)**: 任意数 $N \ge 1$（設定可能）
- **プレイヤブル幅**: 各レーン **幅 3 セル** ($W_{lane} = 3$)
- **境界（ウォール/セパレータ）幅**: 各レーンの間に **幅 1 セル** ($W_{border} = 1$)
- **高さ ($H$)**: 20セル程度（調整可能）

```text
全体幅 = (3 * N) + (N - 1)  [外壁を除く場合]
例: N=3 の場合 (幅 3+1+3+1+3 = 11セル)
| ■ ■ ■ | ■ ■ ■ | ■ ■ ■ |
  Lane0   Lane1   Lane2
 (境界: | )
```

- レーン間の境界（Border）はブロックが侵入できない壁、または視覚的なセパレータとして機能します。
- 各レーンは独立したグリッド状態を保持するか、全体で1つの統合グリッドとして扱いビットマスク等で管理可能です。

---

## 3. ブロック（ミノ）の定義 (Block Specification)

すべてのブロックは **3セル（トミノ / Tromino）** で構成されます。

### 3.1 形状一覧
1. **直線型 (I-Tromino / Straight 3)**
   - セル数: 3
   - 回転状態:
     - 横向き: `[■ ■ ■]`（幅3×高さ1）
     - 縦向き: `[■]` / `[■]` / `[■]`（幅1×高さ3）
2. **L字型 (L-Tromino / Corner 3)**
   - セル数: 3（2×2グリッド内に収まる）
   - 回転状態（4方向）:
     - 0°:  `■ .` / `■ ■`
     - 90°: `■ ■` / `■ .`
     - 180°:`■ ■` / `. ■`
     - 270°:`. ■` / `■ ■`

---

## 4. 自動処理（Auto AI / Auto-Placement）仕様

本システムはプレイヤーによる手動操作だけでなく、**自動で最適位置・向きを判定して処理**します。

### 4.1 探索と評価アルゴリズム
各レーンにブロックが出現した際、そのレーン内（幅3×高さH）におけるすべての合法手（回転状態 × X座標）をシミュレーションし、評価関数に基づき最適な配置を即座またはステップごとに決定します。

#### 評価関数の指標例
1. **ライン消去数 (Lines Cleared)**: 揃って消える行数を最大化 (+評価)
2. **最大・平均接地高さ (Height Penalty)**: 積み上がりの高さを最小化 (-評価)
3. **空洞・穴の数 (Holes Penalty)**: 上にブロックがあるのに下にある空間の発生を防ぐ (-評価)
4. **表面の平坦度 (Bumpiness / Roughness)**: 各列の高さの差を抑える (-評価)
5. **境界連携（オプション）**: 隣接レーンの消去タイミングやボーナス等の拡張

---

## 5. ゲームループと並行処理 (Game Loop & Parallelism)

1. **スポーン (Spawn)**:
   - 各レーンの上部（幅3の中央またはランダム）に3セルブロックを生成。
   - レーンごとに独立したタイマーで落下させるか、全体同期ステップで落下。
2. **自動配置計算 (Auto Solve)**:
   - レーンごとに独立して評価・目標配置（Target X, Rotation）を算出。
   - BevyのECSシステムにより、各レーン・ブロックの更新処理を並列実行可能。
3. **落下 & 固定 (Drop & Lock)**:
   - スムーズなアニメーション落下、または瞬時配置（高速シミュレーションモード）。
4. **ライン消去判定 (Line Clear)**:
   - **レーン内ローカル消去**: 幅3すべてが埋まった行を消去。
   - **グローバル連携消去（拡張案）**: 全レーン横断で揃った場合のメガ消去ボーナス。
5. **ゲームオーバー / スコア集計**:
   - 上限到達時のレーン個別リセット、またはゲームオーバー処理。

---

## 6. 技術スタック & Bevy ECS 設計

- **言語**: Rust (Edition 2024)
- **ゲームエンジン**: Bevy 0.19.x

### コンポーネント設計 (Components)
- `Lane`: レーンのインデックス（`id: usize`）、グリッド状態
- `CellCoord { x: i32, y: i32 }`: セル単位の座標
- `Tromino`: ブロック種別（Straight / Corner）、回転、セル相対位置
- `AutoController`: 自動配置のターゲット位置や探索ステート
- `BoardGrid`: 幅3×高さHのビットボードまたは配列

### システム構造 (Systems)
1. `spawn_trominos_system`: 新規ブロックの生成
2. `auto_ai_decision_system`: 最適な向き・位置の計算
3. `movement_and_gravity_system`: 落下と接地判定
4. `line_clear_system`: 行消去および落下処理
5. `render_grid_system`: 境界およびブロックのメッシュ/スプライト描画
